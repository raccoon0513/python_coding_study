# 26년 3월 4일

### 연습문제 : 헬로월드 출력하기
```python
print('hello world!')
```



# Q1. 등변사다리의 높이

## 문제
길이를 알고 있을 때, 등변사다리의 높이를 구하고 싶어한다. 구름이를 도와 주어진 조건에서 등변사다리의 높이의 제곱을 네 배한 값을 구하여라.

## 입력
첫재 줄에는 등변사다리의 빗변 $K$와 두 대변 $D_1$, $D_2$의 길이가 공백을 두고 주어진다

입력값 : 1 1 1

목표 출력값 : 4

```python
# 문제 1 등변사다리의 높이
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
import math
user_input = input()
K, d1, d2 = user_input.split(" ")
K = int(K)
d1 = int(d1)
d2 = int(d2)
h=0
h = ( K**2 - (abs(d1-d2) / 2 )**2 ) *4
print(int(h))
```

## 풀이과정
등변사다리 높이 공식
$h= \sqrt{K ^2-{(\frac{|D_1-D_2|}{2})}^2}$

결과를 넣었으나 정답으로 인정되지 않았기에

print(h)에 int( )를 씌워 형변환했습니다


# Q2. 택시 요금
## 문제
구름이는 친구들과의 약속을 위해 택시를 타려고 한다. 이때, 처음 거리 $C$ 까지 거리  $1$ 당 요금 $A$ , 그 이후로는 거리 $1$ 당 요금 $B$이다. 구름이가 거리 D만큼 택시를 타게 될 때, 구름이가 지불해야 하는 요금을 알아보자.

## 입력
첫째 줄에 $A,B,C,D$가 공백을 두고 주어진다.

입력 : 5 20 40 91

출력 : 1220

입력 : 64 23 92 86

출력 : 5504

```python
# 문제 2 택시 요금
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
user_input = input()
A,B,C,D = map(int, user_input.split())
pay = 0
if D < C :
	pay = D * A
else : 
	pay = (C * A) + ((D-C) * B)
print(int(pay))
```

# 풀이과정
keypoint : D가 C보다 짧을 수 있음
map() 함수를 통해 미리 입력을 정수형으로 형변환


# Q3. 바느질

## 문제
구름이는 바느질을 하고 있던 중, 실이 조금 부족하다는 것을 깨달았다. 미리 정해둔 바느질 구멍에 따라 순서대로 바느질을 이어가야 하지만, 실의 길이가 모자라기 때문에 바느질 구멍 하나를 건너뛰고자 한다. 처음과 마지막 구멍을 건너뛰면 바느질의 모양이 많이 틀어지므로, 처음과 마지막을 제외한 구멍 하나를 건너뛸 예정이다. 또한, 정확한 실의 길이를 재는 것은 어렵기 때문에, 두 바느질 구멍을 잇는 실의 길이는 바느질 구멍 사이의 좌표 차이의 절댓값을 합한 거리인 택시 거리를 사용하여 계산한다. 바느질 구멍 하나를 건너뛸 경우 구름이가 사용해야 하는 가장 짧은 실의 길이는 얼마인지 구하여라.

## 입력
첫째 줄에는 바느질 구멍의 개수 $N$이 주어진다.
다음 $N$개의 줄에는 바는질 구멍의 좌표 $X_i$, $Y_i$
가 공백을 두고 주어진다.
## 출력
첫째 줄에 바느질 구멍 하나를 건너뛸 경우 구름이가 사용해야 하는 가장 짧은 싫의 길이를 출력

입력값 :
```
4
0 0
8 3
11 -1
10 0
```

목표 출력값 : 14

```python
# 문제 3 바느질
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
# 문제 3 바느질
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
import math
count = int(input())
arr = []
for i in range(count):
	arr.append(list(map(int, input().split())))

def manhattan_dist(p1, p2):
	x1, y1 = p1
	x2, y2 = p2
	return abs(x1 - x2) + abs(y1 - y2) 
	
def remove_test(arr):
	total_distance =0
	for i in range(len(arr)-1):
		total_distance += arr
	return total_distance

full_distance = 0
for i in range(len(arr)-1):
	full_distance += manhattan_dist(arr[i], arr[i+1])

results = []
for i in range(1, count-1):
	before = manhattan_dist(arr[i], arr[i-1]) + manhattan_dist(arr[i], arr[i+1])
	after = manhattan_dist(arr[i-1], arr[i+1])
	results.append(after - before)

print(full_distance + min(results))
```

## 풀이과정
택시 거리 : 맨하탄 거리를 뜻함

처음엔 루프 내에서 루프를 돌렸더니 runtime에러가 발생하여,

한번 루프를 통해 총 거리(total distance)를 구하고,

다른 루프를 통해 새 거리로 대체되었을때 줄어드는 거리(results)를

배열에 저장, min을 통해 가장 많이 빠지는 값을 추적,

full distance에 더하여 값을 도출함.

# Q4. 정수의 길이

## 문제
정수 $N$이 주어집니다. 이때 정수 $N$의 길이를 출력하려고 합니다. 이때 음수의 표현인 $-$도 $1$글자만큼의 길이를 가지고 있습니다.

## 입력
첫째 줄에 정수 $N$이 주어집니다

## 출력
주어지는 정수의 길이를 출력하시오

입력값 : 10

목표 출력값 : 2

```python
# 문제 4 정수의 길이
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

num = input()
str(num)
print(len(num))
```

## 풀이과정
정수를 문자열로 형변환 후 길이를 출력하면 되는

매우 간단한 문제

# Q5. 조별 과제 

## 문제
구름이와 바람이는 새싹 대학교에서 같은 강의를 듣고 있다. 그리고 오늘 둘이서 같은 조를 이루어서 조별 과제를 하게 되었다.

바람이는 구름이보다 과제를 하는 속도가 더 빠르다. 구체적으로, 각자 맡은 파트의 진행된 정도를 수치로 나타낼 때, 구름이는 하루 동안 $G$만큼 올릴 수 있으며,  올릴 수 있다. 이렇게 바람이가 구름이보다 과제를 하는 속도가 더 빠르기 때문에, 각자 맡은 파트의 진행된 정도를 최대한 비슷하게 올릴 수 있도록 다음과 같이 과제를 하기로 했다.

- 각자 맡은 파트에 대해 하루 동안 과제를 한다.
- 구름이의 파트보다 바람이의 파트의 진행된 정도가 더 높다면, 두 사람이 맡은 파트를 서로 바꾼다.
- 구름이의 파트와 바람이의 파트의 진행된 정도가 같다면, 바람이가 구름이의 파트의 진행된 정도를 $1$만큼 올려준다.

조별 과제 발표일까지 남은 일수가 주어질 때, 조별 과제 발표일 때 각자 맡은 파트의 진행된 정도를 알아보자. 이때, 조별 과제를 시작할 때의 두 사람이 맡은 파트의 진행된 정도는 둘 다 $0$이며, 하루도 빠짐없이 두 사람 다 과제를 한다고 가정한다.
## 입력
첫째 줄에 조별 과제 발표일까지 남은 일수 $N$, 구름이가 과제를 하는 속도 $G$, 바람이가 과제를 하는 속도 $B$가 공백을 두고 주어진다.

## 출력

입력값 : 3 6 9

목표 출력값 : 24 22 

입력값 : 5 3 4

목표 출력값 : 20 19

```python
# 문제 5 조별 과제
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
N, G, B = map(int, input().split())

g_progression, b_progression = 0, 0
def exchange():
    global g_progression, b_progression
    g_progression, b_progression = b_progression, g_progression

for i in range(N):
    
    g_progression+=G
    b_progression+=B
    if g_progression < b_progression:
        exchange()
    elif g_progression == b_progression:
        g_progression +=1

print(g_progression, b_progression)
```

## 풀이과정
정답률 0%의 문제

교체를 어떻게 구현할까 고민하다가

exchange 함수를 추가해 주기적으로 변경해주었다

변수 교환 로직에서 파이썬은
temp를 쓰는 대신 a, b= b,a를 사용 할 수 있다

지금생각해보니 함수를 만들 필요는 없던 것 같다

"일단 하루동안 숙제를 한다"

라는 룰을 지키면 크게 어려울 것은 없다

# Q6. DNA 연구소

## 문제
구름 DNA 연구소의 연구원인 바람이는 흥미로운 프로젝트를 맡게 되었다. 바로, 주어지는 **DNA의 가장 기본적인 패턴**을 찾아내는 것이다. 이는 구체적으로 설명하면 다음과 같다.

- 주어지는 DNA는 **알파벳 소문자로만 이루어진 문자열**이다.
- DNA의 패턴이란, **알파벳 소문자로만 이루어져 있음**과 동시에 **DNA의 부분 문자열이 아닌 문자열**을 의미한다.
- 가장 기본적인 패턴이란, **DNA의 패턴 중에서 사전순으로 가장 앞서는 패턴**을 의미한다.

DNA가 주어질 때, **주어지는 DNA의 가장 기본적인 패턴**을 알아보자.
## 입력
첫째 줄에 주어지는 DNA를 의미하는 문자열 $S$의 길이 $N$이 주어진다.

둘째 줄에는 주어지는 DNA를 의미하는 문자열 $S$가 주어진다
## 출력

입력값 : 
```
5
goorm
```


목표 출력값 : `a`

입력값 : 
```
5
baram
```
목표 출력값 : `aa`



```python
# 문제 6 DNA 연구소
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
str_len, str= input(), input()
def get_dna(dna="a"): 
    while(True):
        if dna in str:
			dna += "a"
		else : 
			return dna
print(get_dna())

```

## 풀이과정
정답률 0%의 어려운 문제다.

풀이보단 문제를 이해하는데 한참 걸렸다

사전적으로 앞선다는 뜻은

ab보다 aaa가 먼저라는 뜻이고,

그렇기에 dna가 문자열에 포함되지 않을 때 까지

dna에 a를 계속 더해줬다

# Q7. 최소 연산으로 배열하기

## 문제
정수 배열이 주어졌을 때 다음 중 한 가지 연산을 한 번만 사용하여 배열을 오름차순으로 정렬할 수 있는지 판단하시오. 연산은 다음과 같다.

1. swap : 배열의 두 요소를 스왑한다
2. reverse : 배열의 한 구간을 뒤집는다

다음 조건에 따라 결과를 출력하시오

- 만약 배열이 이미 정렬된 상태라면 첫째 줄에 `yes`를 출력한다. 이 경우 추가적인 출력은 하지 않는다.
- 만약 배열이 한 번의 연산으로 정렬 가능하다면 첫 줄에 `yes`를 출력한다
- 만약 swap으로 정렬이 가능하다면 둘째 줄에 $swap$ $l$  $r$을 출력한다. 이때 $l$과 $r$은 스왑할 두 원소의 인덱스이다.
- 만약 reverse로 정렬이 가능하다면 둘째 줄에 $reverse$ $l$  $r$을 출력한다. 이때 $l$과 $r$은 $reverse$를 적용할 구간의 시작과 끝 인덱스를 나타낸다
- swap과 reverse 두 방법 모두 가능하다면 reverse를 우선적으로 출력한다
만약 두 가지 연산 중 어느 것으로도 배열을 정렬할 수 없다면, 첫째 줄에 `no`를 출력한다 
## 입력
첫째 줄에 배열 $arr$의 길이 $N$이 주어진다
둘째 줄에 $N$개의 정수가 공백으로 구분되어 주어진다. 이 정수들은 배열들의 원소를 나타낸다.
## 출력

입력값 : 
```
6  
1 5 4 3 2 6  
```
목표 출력값 : 
```
yes  
reverse 2 5  
```

입력값 : 
```
6  
3 1 2 4 5 6   
```
목표 출력값 : 
```
no
```

입력값 : 
```
4
1 3 2 4
```
목표 출력값 : 
```
yes
swap 2 3
```

## 1차 과제 제출

```python
# 문제 7 최소 연산으로 배열 정렬하기
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
arr_len = int(input())
arr = list(map(int, input().split()))
def get_count_match(arr, arr_sorted):
    match_checker = [False for _ in range(arr_len)]
    false_index_arr = []
    for i in range(arr_len):
        if arr[i] == arr_sorted[i]:
            match_checker[i] = True
        else :
            false_index_arr.append(i)
    return match_checker, false_index_arr
arr_sorted = sorted(arr)
metch_checker, false_index_arr = get_count_match(arr, arr_sorted)

#전부맞는지체크
sum_metch_checker = arr_len - sum(metch_checker)
start = false_index_arr[0]
end = false_index_arr[-1] +1
if(sum_metch_checker==0):
    print("yes")

#리버스 체크
elif arr[start:end][::-1] == arr_sorted[start:end]:
    print("yes  ")
    print(f'reverse {false_index_arr[0]+1} {false_index_arr[-1]+1}  ')
    
#스왑 체크
elif (sum_metch_checker==2):
    arr[false_index_arr[0]], arr[false_index_arr[1]] = arr[false_index_arr[1]], arr[false_index_arr[0]]
    if(arr ==arr_sorted):
        print("yes  ")
        print(f'swap {false_index_arr[0]+1} {false_index_arr[-1]+1}  ')
    else:
        print("no")



else:
    print("no")
```

## 풀이과정
정답률 0% 난이도 2 문제

문제에 오류가 있는 듯 하여

문의를 남겨두었습니다

예시 3이 reverse가 우선시되어야 하지만

어째서인지 swap이 우선시되어있습니다.

<p style = color:red;>-  26년 3월 6일 추가</p>

문의 답변이 왔습니다

오류를 확인하여 수정했답니다

문제를 다시 확인해보니

예시와 양식이 달라져있었습니다.

## 2차 과제 제출
```python
# 문제 7 최소 연산으로 배열 정렬하기
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
arr_len = int(input())
arr = list(map(int, input().split()))
arr_sorted = sorted(arr)

false_index_arr = []
for i in range(len(arr)):
    if arr[i] != arr_sorted[i]:
        false_index_arr.append(i)

false_len = len(false_index_arr)


def checker():
    #전부맞으면 통과
    if false_len == 0:
        print("yes")
        return
    
    #안맞으면 리턴
    elif(false_len==6):
        print("no")
        return

    
    
    else:
        start = false_index_arr[0]
        end = false_index_arr[-1]

         # 두개면 스왑
        if false_len == 2:
            print("yes")
            print(f"swap {start+1} {end+1}")
            return
        #리버스체크
        reverse_able = True
        for i in range(start, end):
            if arr[start] != arr_sorted[end]: 
                reverse_able = False
                break
        if reverse_able:
            print("yes")
            print(f"reverse {start+1} {end+1}")
            return
        
       
        #아니면 no
        print("no")
        return
        

checker()

```

## 2차 제출 풀이
로직을 수정했습니다

1. 문제 문의 후 swap로직이 reverse보다 우선시됨을 확인
2. false_len이 2라면 arr_sorted랑 비교할 필요 없이 바로 스왑해주기로 변경 ->런타임 에러 해결
3. 이후에 로직을 담아 return함수로 내부함수를 탈출하고 종료하게 변경
4. 문제 해결


# Q8. 큰 팩토리얼

## 문제
팩토리얼 수학 기호는 아래와 같이 정의할 수 있다.

$N! = 1\times 2 \times...\times N$

이때 $N$이 주어지면,  $N!$값을 출력하시오.




## 입력

첫째 줄에 N이 주어진다

- $1 \leq N \leq 100,000$

## 출력

$N!$값을 $1,000,000,007$ 값으로 나눈 나머지를 출력하시오.

입력값 : 3

목표 출력값 : 6

입력값 : 100

목표 출력값 : 437918130


## 1차 풀이
```python
# 문제 8 큰 팩토리얼
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

N = int(input())

def factorial(N, result=1):
    if N!=0:
        result *= N
        result= factorial(N-1, result)    
    return result
    
# print(factorial(N))
print(factorial(N)%1000000007)
```

## 풀이과정
재귀함수를 한번 쯤 배워보고 싶다고 생각했다

이번 기회에 재귀함수를 써서 풀어보려고 했다

그런데 python은 함수의 깊이를 1000번만 허용하기 때문에

재귀함수가 아닌 다른 방법을 찾을 필요가 있었다

## 2차 과제 제출
```python
# 문제 8 큰 팩토리얼
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

N = int(input())

def factorial(N):
    result = 1
    for i in range(1, N+1):
        result *= i
    return result
    
# print(factorial(N))
print(factorial(N)%1000000007)
```

## 2차 문제 풀이
재귀함수를 빼고

for문으로 변경

for문을 사용했기에 runtime 에러가 뜨지 않게 되었다

# Q9. 인공지능 청소기

## 문제
준우는 이번에 홈쇼핑에서 홍보했던 로봇 청소기를 구매했다. 준우는 새로 산 로봇 청소기의 예약 청소 기능을 테스트해 보려고 한다. 예약 청소 기능은 로봇 청소기가 자유롭게 주변을 청소하다가 사용자가 설정한 시간과 장소에 정확히 도착하는 기능이다. 원하는 시간과 장소의 좌표를 입력하면 다른 장소들을 임의로 청소하다가 정확히 도착 예정 시간에 해당 위치에 도착한다. 이 과정에서 동일한 위치를 두 번 이상 지나쳐서 청소할 수도 있다.

준우는 다음과 같은 규칙 하에서 예약 청소 기능이 정확히 동작할 수 있는지 궁금해졌다.

- 준우의 집은 2차원 정수 좌표계로 표현할 수 있고 초기 로봇 청소기의 위치는 $(0,0)$이다.
- 로봇 청소기는 $1$초에 한 번 움직여야만 하며, 이 때 상하좌우 중 하나의 방향으로 $1$의 거리를 움직인다.
- 이미 청소하고 지나갔던 칸을 다시 방문할 수 있다.
- 준우가 정한 $N$초의 시간이 정확히 흐른 시점에 로봇 청소기는 $(X,Y)$에 위치해 있어야 한다.

준우가 예약한 시간과 위치의 좌표가 입력으로 주어질 때, 로봇 청소기가 정확한 시간에 도착할 수 있는지 여부를 판단하는 프로그램을 작성하시오.
## 입력
첫째 줄에 테스트케이스의 개수 $T$가 주어진다.

다음 $T$개의 줄에는 세 정수 $X,Y,N$이 공백을 두고 주어진다.
- $1 \leq T \leq 10$
- $-10^9 \leq X,Y \leq 10^9$
- $1 \leq N \leq 2 \times 10^9$
## 출력
각 테스트케이스에 대한 정답을 한 줄에 하나씩 차례대로 출력한다
- 정확히 $N$초 뒤에 로봇 청소기가 $(X,Y)$좌표에 도착할 수 있다면 `YES`를 출력한다.
- 그렇지 않다면 `NO`를 출력한다


입력값 : 
```
4
-5 -2 7
5 -5 2
0 5 6
1 2 7
```

목표 출력값 : 
```
YES
NO
NO
YES
```

```python
# 문제 9 인공지능 청소기
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
dot_arr = [[0,0]]
def manhattan_distance(a, b):
    return abs(a[0]-b[0])+abs(a[1]-b[1])
T = int(input())
for _ in range(1,T+1):
    dot_arr = [[0,0]]
    X,Y, N = list(map(int, input().split()))
    dot_arr.append([X,Y])
    distance = manhattan_distance(dot_arr[-1], dot_arr[-2])
    if(((distance - N)%2 == 0) & (N>=distance)):
        print("YES")
    else:
        print("NO")
```

## 풀이과정
각 좌표의 거리는 맨허튼 거리로 계산,

1칸을 이동할때 가능한 소요 시간은 1+$2^n$

즉 소요시간 $N$은 $(|X_1-X_2| + |Y_1 - Y_2|) + 2^n = N$

$X,Y,N$을 클래스화하여 관리하려하였으나

배열로 변경

`if(((distance - N)%2 == 0) & (N>=distance)):`

로 변경하여 조건을 한번에 실행하도록 변경

`distance = manhattan_distance(dot_arr[-1], dot_arr[-2])`로 for문을 index로 관리하지 않아도 되도록 하였음

# 10Q. 이진수 정렬

## 문제
$N$개의 10진수 정수가 주어진다. 플레이어에게 정수를 그냥 정렬하는 것은 너무 쉽기 때문에, 아래 기준에 따라 정수를 정렬하기로 한다.

1. 10진수 정수를 2진수로 나타냈을 때, 2진수에 포함된 $1$의 개수를 기준으로 내림차순 정렬한다.
2. $1$의 개수가 같다면, 원래 10진수를 기준으로 내림차순 정렬한다.

플레이어가 정수를 잘 정렬했을 때, 앞에서 $K$번째에 위치한 수는 어떤 수가 될지 구해보자.

## 입력
첫째 줄에 주어지는 정수의 수 $N$과 플레이어가 찾으려는 정수의 위치 $K$가 공백을 두고 주어진다.
둘째 줄에 정수 $a_1, a_2...a_N$이 공백을 두고 주어진다.

- $1 \leq N \leq 500,000$
- $1 \leq K \leq N$
- $1 \leq a_i \leq 2^{20}$


## 출력
기준에 따라 정렬된 정수 중, 앞에서 $K$번째에 위치한 수를 출력한다.

입력값 : 
```
8 6
1 2 3 4 5 6 7 8
```
목표 출력값 : `4`

입력값 : 
```
7 1
1 2 4 8 16 32 64
```

목표 출력값 : `64`

```python
# 문제 10 이진수 정렬
# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
import sys
N, K, *arr = list(map(int, sys.stdin.read().split()))

def binary_counter(num):
    tempnum = num
    count = 0
    while(num > 0):
        if num%2:
            count+=1
        num //=2
    return count, tempnum

arr.sort(key = binary_counter, reverse=True)
print(arr[K-1])
```

## 풀이과정
입력받을 값이 많아서

sys.stdin.read()를 통해 여러 줄을 한번에 입력받게 함

(런타임 에러 방지)

arr.sort()의 key 파라미터에 lambda를 쓰는 대신

binary_counter을 정의하여 전달

binary_counter의 리턴값을 두개로 만들어

sort 기준을 1순위 이진수 중 1 개수

2순위 10진수로 설정

입력받은 K가 인덱스가 아닌

~번째 수라서 K-1을 실시

# Q8. 

## 문제

## 입력


## 출력

입력값 : 

목표 출력값 : 

```python

```

## 풀이과정